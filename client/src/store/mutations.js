export const setActiveAccount = (state, account) => {
  state.activeAccount = account;
};

export const setTasks = (state, tasks) => {
  state.tasks = tasks;
};

export const setUser = (state, user) => {
  state.user = user;
};
